# ---- Meta developer app (ONE-TIME setup by the server owner, see README step 1) ----
# Enables the "Continue with Instagram" one-click login for all users.

# >>> REQUIRED: this is the actual fix for "Invalid platform app" <<<
# This is a DIFFERENT ID/secret than your main app's Basic Settings.
# Find it at: App Dashboard -> Instagram product -> "API setup with
# Instagram login" -> look for "Instagram app ID" / "Instagram app secret".
IG_APP_ID=1425765716145104
IG_APP_SECRET=e8d25d051999d5490223afc92d4cb9bc

# Kept as fallback only — NOT used for the Instagram business-login endpoint,
# which is why login was failing with "Invalid platform app" using these alone.
FB_APP_ID=1649439706778137
APP_SECRET=23027245b2a2cd37394a432700932feb

# ---- Public URL of this server (needed for OAuth + webhooks) ----
# e.g. https://your-app.onrender.com or https://xxxx.ngrok.io
WEBHOOK_BASE_URL=http://localhost:5000

# Optional: override the OAuth redirect URI (must match Meta app settings exactly).
# Default is {WEBHOOK_BASE_URL}/api/oauth/callback
# OAUTH_REDIRECT_URI=https://your-app.onrender.com/api/oauth/callback

# ---- Webhook verify token (you choose this; enter the same value in Meta dashboard) ----
WEBHOOK_VERIFY_TOKEN=myinsta_webhook_9f3a2k7x

# ---- Manual-connect fallback only (NOT needed when OAuth is configured) ----
# IG_USER_ID=
# IG_ACCESS_TOKEN=
